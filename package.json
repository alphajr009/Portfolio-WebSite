{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install --legacy-peer-deps",
    "build3": "cd client && npm install vite --legacy-peer-deps",
    "install-client": "cd client && npm run build ",
    "heroku-prebuild": "export NPM_CONFIG_PRODUCTION=false; npm run build3 && npm run build --only=dev",
    "heroku-postbuild": "npm run install-client",
    "start": "node index.js",
    "client": "cd client && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}